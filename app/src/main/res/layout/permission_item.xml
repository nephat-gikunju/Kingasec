<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="12dp"
    android:background="?attr/selectableItemBackground">

    <!-- Permission Icon -->
    <ImageView
        android:id="@+id/imageView_permission_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginEnd="12dp"
        tools:src="@drawable/ic_contacts_small"
        android:contentDescription="Permission Icon"/>

    <!-- Permission Details -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/textView_permission_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Permission Name"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#333333"
            tools:text="Access Contacts"/>

        <TextView
            android:id="@+id/textView_permission_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Permission description"
            android:textSize="12sp"
            android:textColor="#666666"
            android:layout_marginTop="2dp"
            tools:text="Read your contacts and call history"/>
    </LinearLayout>

    <!-- Risk Indicator -->
    <View
        android:id="@+id/view_risk_indicator"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/circle_shape"
        android:backgroundTint="#D32F2F"/>
</LinearLayout>

